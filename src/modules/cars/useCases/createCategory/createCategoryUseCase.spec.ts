describe("Criar categoria", () => {
  it("Espero que 2 + 2 seja 4", () => {
    const sum = 2 + 2;

    expect(sum).toBe(4);
  });

  it("Espero que 2 + 2 não seja 5", () => {
    const sum = 2 + 2;

    expect(sum).not.toBe(5);
  });
});
